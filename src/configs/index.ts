import cookies from "@configs/cookies";
import publicRuntime from "@configs/publicRuntime";
import routes from "@configs/routes";

const config = {
    cookies,
    publicRuntime,
    routes,
};

export default config;
