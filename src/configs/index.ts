import cookies from "@configs/cookies";
import localStorages from "@configs/localStorages";
import publicRuntime from "@configs/publicRuntime";
import routes from "@configs/routes";

const config = {
    cookies,
    localStorages,
    publicRuntime,
    routes,
};

export default config;
